The PathLens frontend has been deployed.

Service: {{ include "pathlens.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

{{- if .Values.ingress.enabled }}
Ingress host: http://{{ .Values.ingress.host }}{{ .Values.ingress.path }}
{{- else if eq .Values.service.type "NodePort" }}
Run: minikube service {{ include "pathlens.fullname" . }} -n {{ .Release.Namespace }} --url
{{- else }}
No ingress is enabled. Reach the service from inside the cluster, or run:
  kubectl port-forward svc/{{ include "pathlens.fullname" . }} 8080:{{ .Values.service.port }} -n {{ .Release.Namespace }}
{{- end }}
